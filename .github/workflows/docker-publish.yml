name: CI/CD Pipeline - Docker Hub

on:
  push:
    branches:
      - main
  workflow_dispatch: # Ne permite să pornim procesul și manual din interfața GitHub

jobs:
  build-and-push:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest

    steps:
      - name: Descarcă codul sursă
        uses: actions/checkout@v4

      - name: Autentificare in Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build si Push in Docker Hub
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          # Generează automat tag-ul ex: ananas07/tennis-auth:latest
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/${{ github.event.repository.name }}:latest